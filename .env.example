# Environment configuration for development
PORT=8080
DEBUG=true

# PostgreSQL
POSTGRES_URL=postgres://postgres:postgres@localhost:5432/riskdb?sslmode=disable
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=riskdb

# Redis
REDIS_URL=localhost:6379

# ML Services
ANOMALY_SERVICE_URL=http://localhost:5001
RISK_SERVICE_URL=http://localhost:5002

# ML Model paths (optional, uses demo weights if not set)
# ANOMALY_MODEL_PATH=/path/to/model.pt
# RISK_MODEL_PATH=/path/to/model
